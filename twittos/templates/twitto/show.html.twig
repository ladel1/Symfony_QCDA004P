{% extends 'base.html.twig' %}

{% block title %}Twitto{% endblock %}

{% block body %}
    <h1>Twitto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ twitto.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ twitto.content }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ twitto.image }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ twitto.createdAt ? twitto.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_twitto_index') }}">back to list</a>

    <a href="{{ path('app_twitto_edit', {'id': twitto.id}) }}">edit</a>

    {{ include('twitto/_delete_form.html.twig') }}
{% endblock %}
