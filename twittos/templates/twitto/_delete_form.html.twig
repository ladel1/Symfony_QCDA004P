<form method="post" action="{{ path('app_twitto_delete', {'id': twitto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ twitto.id) }}">
    <button class="btn">Delete</button>
</form>
